%nav.nav
  .nav-header
    Todous
  .dropdown-toggle
    .dash
    .dash
    .dash
  %ul.dropdown.right   
    - unless current_user 
      %li= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path
    - else
      %li= link_to "Sign out", destroy_user_session_path, method: :delete
    - if current_user        
      %li
        .profile
          %span= link_to "#{user.name}", user
          %img.picture{:src => user.image} 
